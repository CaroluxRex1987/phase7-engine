But trend_health.py itself never raises — it returns default_response with 0.0 and degraded_inputs. When would compute_trend_health raise? It catches its own exceptions. The engine_core check if not isinstance(trend, dict) or "trend_health" not in trend: raise ValueError — that raise would be caught by this except → trend_health 50.0 with NO degradation recorded (this except doesn't append to degradation!). trend_health.py's default_response has degraded_inputs... wait no, if compute_trend_health returns its default_response, it HAS "trend_health" key (0.0), passes the isinstance check, and degradation.extend(trend.get("degraded_inputs", [])) records it. Good. The engine_core except only fires if compute_trend_health itself raises (basically impossible — it has a broad internal try) or returns a non-dict (impossible) or a dict without "trend_health" (impossible — both return paths include it). So the 50.0 fallback is essentially dead code — but it's the exact 50.0-center-of-scale fabrication item 9a says was removed, still sitting in engine_core, and unlike the module it lacks the degraded_inputs recording. Latent (unreachable in practice). Observation: the fabricated 50.0 survives in engine_core's except, unreachable because the callee never raises; if a future change made it reachable, a failure would present as a mid-scale reading with no degradation. Minor/latent.

calculate_dynamic_regime: vol_ratio = 0.01  # Default to medium volatility — if ATR NaN at last bar... but indicators guarantee ATR finite at last bar or the run already halted (section 2 raises if ATR column missing; the sweep drops it if trailing-NaN). Actually wait — ATR present but price NaN? close is validated upstream. So 0.01 default is nearly unreachable. But note: vol_ratio exactly 0.01 → vol_ratio > 0.01 False → LOW VOLATILITY, not MEDIUM! The comment says "Default to medium volatility" but 0.01 falls into the LOW bucket (else branch: vol_ratio > 0.01 is False, > 0.02 False, > 0.04 False → LOW VOLATILITY). Comment wrong about its own fallback — tiny, but the instruction says comment-wrong-about-code is a finding. It's a comment inaccuracy on a near-dead path. Also: ATR absent entirely → volatility_mode = "UNKNOWN" (fine, honest).

BiasStateMachine.transition — pure logic, fine.

live_trading.py: .get(..., default) fallbacks building the simulated order — e.g., "atr_stop": risk.get("atr_stop", 0.0), targets default (0,0,0), current_price default 0.0. If the decision object lacks these (can't happen for a successful result — run_once checks error first... but a successful result always has these keys from the router). Latent defaults that would print zeros. Also datetime.datetime.utcnow() — deprecated in 3.12 but works. And live_trading_simulator = LiveTradingSimulator() at module scope — constructs SignalRouter → Phase7Engine at import time; also os.makedirs(self.log_dir, exist_ok=True) at import → importing live_trading creates a directory (side effect at import). test_imports imports live_trading — creates logs/LiveSim on the test machine. Minor.

data_fetcher.fetch_ohlc — MEXC klines: df = pd.DataFrame(data, columns=[...8 fields...]) — if MEXC returns a different field count, DataFrame constructor raises → caught? The try only wraps requests.get/raise_for_status/json. The DataFrame construction is OUTSIDE the try. If data rows have ≠8 fields, pd.DataFrame(data, columns=[...]) raises ValueError — uncaught in fetch_ohlc → propagates to get_tf → propagates to engine_core's get_tf call → caught by engine_core's outer try → "Critical error in Phase7Engine pipeline". So a malformed API response (wrong field count) escapes the nice fetch_error channel and becomes a generic critical error. Also rows with 8 fields but non-numeric strings: astype(float) raises — also outside try → same path. Actually the outer engine_core catch handles it as an error return, so the run fails closed with a traceback logged. Not a fabrication, but the A13 "distinguish fetch failure" channel is bypassed for malformed-shape responses. Also: no check that data rows are lists of len 8. Minor observation.

Also fetch_ohlc doesn't pass now... it does: validate_ohlcv(df, timeframe, now=...). Good.

Hmm, also requests.get(url, params=params) — no timeout! A hung connection hangs forever. Minor.

VWMA: df["VWMA"] = np.where(valid_mask, ...) then if df["VWMA"].isna().any(): df["VWMA"] = df["VWMA"].ffill() — forward fill only, trailing NaNs remain NaN (ffill leaves trailing NaN). Wait — ffill leaves TRAILING NaN? No: ffill fills forward from last valid; NaNs AFTER the last valid value remain NaN. Yes trailing stays NaN. Good. But there's a subtle: df["VWMA"] = np.where(...) produces a numpy array assigned to column — dtype float64, fine.

Volume spike detection: appended as IndicatorFailure → degradation → run degraded → NO-TRADE. The comment says "the spike's real value reaches every calculation UNALTERED" — true. And degraded. Consistent with the ruling as described.

BUT WAIT — there's something: the spike check compares finite_recent > SPIKE_RATIO * baseline_median where baseline_median is the median OF THE SAME WINDOW INCLUDING THE SPIKE. "one extreme value cannot drag its own baseline away from itself the way a mean would" — true for a single spike in a 20-window. OK.

But hmm — every run on real crypto data: is a 10x spike common? On low-liquidity pairs, volume spikes of 10x median over 20 candles happen regularly (e.g., a single whale candle). Then every such run is degraded → NO-TRADE always. That's an availability trade-off, ruled by Viktor per comments. Not a finding against code truthfulness. Skip (or mention in Part 6).

decision_log.write — swallows all exceptions → None; panel then prints "Decision NOT logged". Consistent.

lineage.prune — deletes archives >90 days by mtime. ARCHIVE_RE = r"^[a-z0-9]+_[a-z0-9]+_[0-9a-f]{16}\.json\.gz$". archive_name: f"{safe(symbol)}_{safe(timeframe)}_{run_id[:16]}.json.gz" — safe() strips non-alphanumeric and lowercases. symbol "AEROUSDT" → "aerousdt", timeframe "4h" → "4h". Matches pattern. OK. Uses file mtime — a restored/copied archive resets mtime → kept longer; a wrong system clock → wrong pruning; keep= protects current run. Reasonable.

lineage.canonical_text — deterministic; _index_label for tz-aware: label.tz_convert("UTC") — for a pandas Timestamp, tz_convert works; for python datetime, hasattr tz_convert False → astimezone(timezone.utc). OK.

One subtle thing: canonical_text writes out.write(f"phase7-canonical-v{CANONICAL_FORMAT}\n") then header index + columns. rebuild_frame parses. Round-trip of floats via repr — exact. Timestamps: ISO format; rebuild via pd.to_datetime(pd.Index(labels), errors="coerce", format="ISO8601"). For tz-aware UTC labels with "+00:00" — parses fine. But micro/nano precision: isoformat of a Timestamp includes microseconds if present. pd.to_datetime with format="ISO8601" handles. Then frame_hash(rebuilt) == original? The canonical text of rebuilt frame: index labels — rebuilt index is DatetimeIndex; isoformat of parsed timestamps should reproduce the same string if the parse preserves precision. pandas 3 is microsecond resolution; if original was nanosecond... ns precision could be lost in pandas 3. They pinned pandas==3.0.5. Hmm — if the original index has nanosecond components (rare for candle data, which is ms), isoformat would include fractional seconds beyond micro? Timestamps from ms epochs have 0 microseconds — fine for this engine.

rebuild_frame epoch conversion in test_lineage: out.index.astype("datetime64[ms]").astype("int64") — test-side. Fine.

7.4 — restatement of inputs.

confidence = abs(bias_score) only. bias_score includes macro at 0.10. Validation score separately computed from macro agreement + volume sentiment — printed as VALIDATION line. Validation does NOT feed confidence anymore. But validation_state DOES feed _determine_final_action: if validation_state == "WEAK" and trend_health < 40: return "WAIT". So validation (macro agreement + volume sentiment — both already weighted inside bias_score at 0.10 and 0.15) is used as a gate in the decision, on top of bias_score driving raw_bias which drives the direction blocks. Is that double counting? validation_state == WEAK requires val_score < 45: e.g., macro disagrees (-20) and volume divergence (-25) → 50-45 = 5 → WEAK. So a WEAK validation means macro and/or volume disagree — and those already pulled bias_score down. The WAIT gate then blocks based on them again (combined with trend_health < 40 — trend health is also in bias at 0.30). Hmm — it's a second use of the same evidence in the decision path: bias_score already reflects macro+volume+trend_health; raw_bias already encodes their net; the validation gate re-applies macro/volume/trend_health as a separate veto. The instruction 7.4: "Where two values feed one score, check whether they are independent measurements or the same measurement twice. Check also whether a factor already weighted inside a composite is then applied a second time on top of it." validation_state's components (macro agreement, volume sentiment) are weighted inside bias_score and then validation_state is applied as a gate affecting the same decision. That looks like the same evidence applied twice in one decision path. Counter-argument: a veto gate and a weight are different mechanisms; bias_score blends, validation vetoes extreme disagreement. But the 2-Sept ruling removed macro's second vote for exactly this reason ("Macro has already been counted once, as a 10% weighted factor inside bias_score. A second, overriding vote is the same evidence twice."). The validation gate re-introduces a macro (and volume) override — in the WAIT direction. E.g., strong bullish everything but macro bearish → validation WEAK-ish... let me compute: macro disagrees: -20 → val 30+15(volume strong)=45 → NEUTRAL. WEAK needs <45: macro disagree + volume divergence: 50-20-25=5 → WEAK. So WAIT requires BOTH macro disagreement AND volume weakness AND trend_health<40. All three already weighed in bias. With all three bearish, bias_score would be low anyway → raw_bias likely not BULLISH → WAIT via the direction blocks anyway. So the gate mostly fires when bias is NEUTRAL-ish... it changes outcomes only in a corner. It's a mild double-count, direction-safe (it vetoes rather than authorizes). Worth an observation under 7.4 — same evidence family applied twice in one decision, albeit as a veto. I'll note it measuredly.

Entry quality multipliers: macro_bias, trend_direction, structure_sequence multiply entry score. macro_bias is also in bias_score (0.10); trend_direction derives from EMA slopes which also feed trend_health (0.30 of bias); structure_sequence feeds the CHOCH discount in bias. So entry quality shares inputs with bias_score — acknowledged in decision_model's own comment ("entry_score already shares macro_bias, trend_direction and structure_sequence with bias_score"). And then _determine_final_action requires BOTH bias alignment AND entry_score >= 70 for AGGRESSIVE. Two gates reading overlapping evidence. By design (entry quality is about the entry, bias about direction) — but the overlap means the same macro reading boosts both the direction score and the entry score. It's declared in comments. The instruction 7.4 asks to check; the code itself discloses it. Observation, mild.

BTC-adjusted confidence: uses bias.score (aero_dir) + btc bias + correlation — it's explicitly a separate display number, never replacing confidence. Fine.

7.5 — lookahead.
clean_series: forward only now; interpolate limit_direction='forward'; trailing edge left NaN. pct_slope: slope uses shift(1)/diff — past-looking. OK.
indicators batch clean: ffill with trailing-edge NaN restoration. OK.
plotting bfill — chart only, exempted and pinned by test. OK.
compute_correlation_beta: positional alignment (not lookahead per se, but misalignment — noted above).
structure._find_confirmed_swings: a swing high at bar i requires bars i-lookback..i+lookback — i.e., confirmed only lookback bars later. The most recent confirmable bar is n-1-lookback. So pivots use only data up to "now" for confirmation of older bars — the confirmation window for bar i uses bars AFTER i but those bars are ≤ now (i ≤ n-1-lookback). At decision time (bar n-1), all data used is ≤ n-1. No future info relative to the decision bar. Correct fractal logic. BUT for a backtest walking backwards it would still be fine since it only uses ≤ current bar... Actually the pivot at i uses bars up to i+lookback; if decision bar is d, pivots searched only for i ≤ d-lookback, so window max index = d. Fine — no leak beyond decision bar even in walk-back. Good design.
Volume profile: uses whole frame up to last bar. Fine.
lineage/archive — not decision-feeding. Fine.
EMA/RSI/etc standard backward-looking windows. Fine.
_volume_sentiment_simple uses last 10 bars only. Fine.
So 7.5: no live lookahead found. The bfill exemption in plotting reaches only the chart. The swing confirmation is correctly bounded. Good — say so.

7.6 — same thing computed in two places without comparison.
THE big one the instruction warns about. Let me hunt:

a) Direction: bias (raw_bias from bias_score) vs risk plan direction (detailed_bias → effective_bias in calculate_stop_targets). The 2-Sept fix added _refuse_incoherent_plan comparing action vs plan direction (targets-derived). But note: the plan's direction comes from detailed_bias (via effective_bias = LONG if bias_score >= 0 else SHORT when detailed_bias not in ["LONG","SHORT"]). Wait — calculate_stop_targets: effective_bias = detailed_bias; if effective_bias not in ["LONG","SHORT"]: effective_bias = "LONG" if bias_score >= 0 else "SHORT". detailed_bias is like "BULLISH CONFIRMED" — never "LONG"/"SHORT" — so effective_bias = LONG iff bias_score >= 0. And raw_bias = BULLISH iff bias_score > 20. So plan direction = sign(bias_score), action direction = raw_bias threshold (±20). Both derive from bias_score now — consistent source. If bias_score > 0 but ≤ 20: plan LONG, action: raw_bias NEUTRAL → no LONG/SHORT block → WAIT (unless validation WAIT). So no contradiction: WAIT with a long-leaning plan — but the plan levels are printed anyway (STOP LOSS below price for a LONG-leaning neutral bias). The guard only fires for LONG/SHORT actions. Neutral-bias runs print directional stops/targets computed from sign(bias_score). Hmm — is that a contradiction? The plan leans long while decision is WAIT. The panel prints levels without a direction label attached to the plan per se... but targets ascending = long plan, and the action is WAIT. The operator sees WAIT and levels. Acceptable? The levels' direction isn't labelled; but "TARGET 1 (Cons): $0.96" above price with stop below implies long. On a NEUTRAL bias the engine prints a long-shaped plan (if bias_score ∈ (0,20]) or short-shaped (if [-20,0)). Run 2 transcript: bias BEARISH CONFIRMED... let me check run 2: decision WAIT, stop 0.8935 ABOVE current 0.8617, targets BELOW (0.83, 0.7983, 0.7666) — short-shaped plan with WAIT. Consistent with bias BEARISH (bias_score -52.64 < -20 → BEARISH; plan SHORT). OK consistent.

Run 1: NO-TRADE (RISK TOO HIGH), bias BULLISH CONFIRMED, stop 0.6422 below price 0.8017, targets above — long plan, bullish bias, consistent; risk_valid False (stop distance 19.9%? let me check: (0.8017-0.6422)/0.8017 = 0.199 → 19.9% > 15% → risk fail. Yes consistent with "Stop distance exceeds 15%").

b) Two HVN computations: engine_core section 6: hvn = float(df_struct["HVN"].iloc[-1]) if "HVN" in df_struct.columns else float(structure_obj.get("hvn", 0.0)). df_struct HAS HVN column (written by calculate_structure wrapper: df_clean.loc[:, "HVN"] = result.get("hvn", 0.0)). So hvn = HVN column last value = same value as structure_obj["hvn"] (written into every row). Consistent — same source. OK.

c) current_price computed twice: section 8 current_price = float(df_struct["close"].iloc[-1]) and exit dict uses the same variable. Single source now. OK.

d) trend_direction: computed in trend_health (sign of slope) and bias_engine's trend_direction = sign(continuation_strength). continuation_strength's direction = sign(trend_slope) too (direction = 1/-1/0 from trend_slope sign; continuation_strength = direction * magnitude). So bias_engine's trend_direction = sign(continuation_strength) = sign(trend_slope) when continuation ≠ 0; if continuation_strength == 0 (e.g., slopes exactly 0 or... continuation_strength = direction * max(0, min(100, raw)) — if direction=0 → 0; also if raw_continuation negative? max(0,...) floors at 0 → continuation_strength = 0 even for direction ±1! raw_continuation = adx + momentum + accel_component; accel_component can be negative (decelerating): e.g., adx 25 + rsi 15 + accel -20 (tanh(-large)20 → -20) = 20 → positive. accel min is -20, so raw min = 0+0-20 = -20 → floored to 0 → continuation_strength = direction0 = 0 → trend_direction = 0 in bias_engine → signed_trend_health = 0! So a strong trend with decelerating acceleration and low ADX gets trend_health contribution ZEROED in bias_score (trend factor = ±trend_health * 0.30 → 0). And trend_direction label in trend dict says BULLISH/BEARISH (from slope sign) while bias treats direction as 0. Two direction computations: trend_health.trend_direction (slope sign) vs bias_engine trend_direction (sign of continuation_strength, which floors at 0). They can disagree: slopes positive but accel strongly negative + low ADX/RSI components → continuation 0 → bias sees direction 0 → trend health contributes NOTHING to bias. And signed trend health = 0 also when continuation_strength = 0 for direction=0 (flat slopes) — fine. Is this a defect? It's an edge: strong trend (health 95) but decelerating → bias ignores trend health entirely. The comment in trend_health says "Uses the exact same sign-of-slope test as continuation_strength above, so the two never disagree." — the two = trend_direction label and continuation's direction. True: label = sign(slope), continuation direction = sign(slope) but VALUE floored at 0. So label BULLISH + continuation 0 possible → bias trend_direction = 0 ≠ label BULLISH. The comment's "never disagree" is about label vs continuation's direction field... continuation_strength = direction * max(0, clipped raw). If raw clipped to 0, continuation = 0.0, and sign(0) = 0 ≠ direction. The comment says the label uses the same sign test "so the two never disagree" — but bias_engine derives its trend_direction from continuation_strength's SIGN, which is 0 when floored. So label (BULLISH) vs bias's trend_direction (0) CAN disagree. Effect: trend health zeroed in bias on decelerating trends. Is that intended? Maybe (deceleration = trend not continuing). But then raw_bias = NEUTRAL likely (trend 30% zeroed), and plan direction from sign(bias_score). It's a subtle double-derivation with a floor-induced disagreement — worth an observation under 7.6. Severity: minor-to-moderate; it zeroes a 30% factor in a specific state without any note to the operator. Hmm, actually wait — is it wrong? If the trend is decelerating hard, maybe zeroing is a defensible design. The issue is the comment claims the two directions "never disagree" and they can (label BULLISH while bias direction proxy is 0). Also panel prints TREND: BULLISH while bias treats trend as directionless. Observation.

e) Risk regime: classify_risk_regime called once in validate_risk_parameters, returned, carried. Single source. OK. But note: decision_model defaults risk_regime = str(risk.get("risk_regime", "NORMAL RISK")) — if engine's risk dict lacks it (engine always sets it), default NORMAL — fine, router passes engine's.

f) Stop distance: computed in risk_model (stop_distance for targets), validate_risk_parameters (stop_dist_pct), panel (stop_distance for R:R). Three computations of the same quantity from the same inputs (current_price, atr_stop) — same formula |price-stop|. Consistent inputs → consistent results. R:R ratios printed "R:R 1 : 1.00" — hmm interesting: the panel computes rr_t1 = |t1-price|/stop_distance; risk_model builds t1 = price ± stop_distance*1.0, so R:R is exactly 1.00/2.00/3.00 by construction — the printed ratios are constants. Not a defect, just theatre (printing a computed-looking ratio that's always 1/2/3). Observation, cosmetic.

g) correlation computed in btc_context (positional) — single source. fine.

h) swing_struct: structure.py computes nearest swing; panel prints with "(Lookback 8)" from config.STRUCT_LOOKBACK — if config changes, engine passes lookback=config.STRUCT_LOOKBACK — consistent. OK.

i) VWMA computed in indicators (20-length) and volume sentiment uses its own np.average over 5-candle windows — different quantities, fine.

j) The two state machines (bias vs btc_bias) — separate instances. fine.

k) macro_bias computed once in engine_core. fine.

l) Two EMA-50 reads for macro: macro_bias = close vs EMA_50 on daily. Entry zone on 4h = EMA_20..EMA_50. Different timeframes. fine.

m) The 2-Sept defect class — direction sources — appears handled (single source: raw_bias; plus the coherence guard). Let me verify _refuse_incoherent_plan is actually invoked in evaluate(): yes, final_action = self._refuse_incoherent_plan(final_action, risk, reasons) before confidence. And degradation applied after — degradation can turn LONG into NO-TRADE (DEGRADED INPUT); the refusal happens before degradation, fine.