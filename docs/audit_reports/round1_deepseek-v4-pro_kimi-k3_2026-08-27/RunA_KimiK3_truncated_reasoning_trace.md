# Run A (first attempt) — Kimi K3 reasoning trace

OpenRouter, 2026-08-27. Truncated at 16,384 output tokens; produced no findings.
Recovered verbatim from the session transcript after the OpenRouter room was deleted.

---

Kimi K3
Reasoning
Let me carefully work through this audit task. I'm being asked to perform an independent compliance audit of a crypto trading signal engine ("Phase-7 Structural Quant Engine") against a 44-rule "Engineering Constitution."
First, a critical disclosure I need to make upfront: I am Kimi K3, made by Moonshot AI. The Constitution's Version History (AUDITOR row, August 27, 2026) names "Kimi K3 (Moonshot AI) as the intended primary" auditor. So I am literally the intended auditor named in the document. That's worth noting — it satisfies the "no shared authorship" requirement (I didn't write the code or the Constitution — Claude did), but I share training-data-era habits with other LLMs, so I should flag correlated-reasoning risks where they arise.
The task structure:

1. Begin with the Minimum Viable Audit gate — Items 2, 3, 6, 18.
2. Work through remaining Tier 1 invariants (1, 4, 5, 7-17, 19-21).
3. Then Tiers 2-4.
4. Record findings in the specified schema.
5. Resolve the DEFECT row contradiction about the MVA gate composition (Next Steps says 2, 3, 6, 18; the audit-independence safeguards say "Minimum Viable Audit gate items (Items 2, 3, 6)").
6. Test the two open questions in Section 5 (BTC-Adjusted Prediction epistemic status; Roadmap Layers 2/5) — do not confirm them.
7. Flag correlated reasoning where relevant.

Let me now actually audit the code. I have the full source in front of me. Let me trace through carefully.
MATERIALS CHECK:

* Complete engine source: 19 Python files provided. ✓
* Constitution: provided. ✓
* Step 2a audit package: provided. ✓
* Runtime logs: provided (phase7_engine.log excerpt). ✓
* One simulated trade record: provided (sim_trade JSON). ✓
* Also: scan_summary_report.txt content provided, and the state JSON file.

Wait — the scan summary report is dated 2026-08-24 01:41:52, but the panels it shows look like an OLDER version of the panel (no Decision Reasoning section, no Exit Watch section, no BTC Market Context section, "CONFIDENCE" without "(decision)", and it shows "SWING STRUCT: $77535.4500 (Lookback 8)" where swing_struct equals current price — consistent with the pre-B2 stub). So the scan report predates the C1/C3/B2 builds. Also notably, the scan report shows "R:R 1: 0.68" values — consistent with pre-A12 fix where targets were fixed ATR multiples, giving R:R below 1:1. So the scan summary is from an old build. Good — I should note that the scan report cannot be used as evidence of current behavior.
The runtime log (phase7_engine.log) spans 2026-08-24 to 2026-08-26. It shows various errors during development (fillna method errors — pandas 3.0 removing the `method` kwarg, syntax errors, import errors, NameErrors for Optional/Any). These are from the development period. The most recent runs (2026-08-26) complete successfully.
The simulated trade record is dated 2026-08-25T02:45:02. It shows a NO-TRADE decision with long_signal: true, risk_valid: false, risk_reason "Stop distance exceeds maximum allowable threshold (15%)." Interesting: entry_zone lower (0.4966) > upper (0.4671) — inverted! In engine_core.py, entry_zone_lower = EMA_20 and entry_zone_upper = EMA_50. In a bullish configuration, EMA20 > EMA50, so "lower" (EMA20) > "upper" (EMA50). In entry_model.py there's a guard that swaps them if lower > upper, but the raw values written into the decision object's entry dict are unswapped. So the panel displays ENTRY ZONE: $0.4966 - $0.4671 (inverted) and live_trading logs it inverted. That's a real bug worth noting under traceability/semantics — the "zone_lower"/"zone_upper" labels don't reliably mean lower < upper. Actually in the AERO scan (1d) the entry zone shows "$0.4386 - $0.4408" (lower < upper) but in BTC 4h "$75661.0622 - $71676.9570" (inverted). So the panel sometimes displays an inverted entry zone. This is a minor display defect — EMA20 > EMA50 in uptrends, so the "lower"-"upper" naming is wrong for bullish regimes. It's a real, checkable finding: entry_model swaps internally, but the decision object and panel don't.
Let me now go through the audit systematically.
MVA GATE:
Item 2 — No Future Information / Look-Ahead Bias.
Key checks:

* Indicators: pandas_ta EMA/RSI/ADX/ATR/SuperTrend are all trailing computations; `.iloc[-1]` is used for the latest values. No centered windows visible.
* Structure engine `_find_confirmed_swings`: uses fractal pivots requiring `lookback` bars on both sides, and only considers bars at least `lookback` back from the most recent bar as confirmable ("a pivot can't be confirmed until lookback bars of price action have passed after it"). This is exactly the correct anti-look-ahead treatment for swing detection. ✓ This is genuinely good.
* One subtlety: `window_high = highs[i - lookback: i + lookback + 1]` uses future bars relative to pivot i to CONFIRM it, but the pivot is only used at the present bar, at which point those "future" bars are in the past. Since decisions are made only at the last bar, and the confirmation window extends only to bars that have already occurred (i + lookback <= n-1 enforced by the `if i + lookback >= n: continue` guard), there's no leak into the current decision. ✓
* compute_correlation_beta: uses tails of both series and pct_change; aligns by position after reset_index. Note: AERO and BTC series are both 4h candles from the same exchange, so positional alignment is probably fine, but if the two fetches return different lengths or missing candles, positional alignment could mismatch timestamps — a data-integrity concern (Item 3) more than look-ahead.
* Volume profile: uses the whole window of 450 candles, including the most recent — fine for a "current state" reading.
* Macro bias: uses df_macro's last close vs EMA50 — the last macro candle is the CURRENT, possibly incomplete daily candle. Hmm — for a 4h decision, the "1d" macro candle that's still forming is known at decision time (its close so far is the current price). Not look-ahead.
* BUT: one real look-ahead-adjacent issue — the engine's decision uses the most recent candle's close, which for a 4h candle mid-formation is the live price. That's standard for live tools, not a violation.
* Backtesting doesn't exist yet, so the classic backtest leak can't be assessed. Item 2 as applied to the current live-analysis engine: the only code paths are live snapshot analysis. I should check for any use of future data... The `_detect_sequence` uses confirmed swings only. Good.

One thing to verify: in `add_technical_indicators`, `.ffill().bfill()` — bfill (backward fill) propagates FUTURE values into PAST NaN positions. For a live snapshot where only `.iloc[-1]` matters, bfill affects only early rows (warmup), which don't influence the last row... unless the last row itself is NaN and gets bfill'ed — bfill of the last row pulls from... nothing after it, so ffill handles it. Actually bfill only fills a NaN with a LATER value. For the final row, no later value exists, so the final row can only be affected by ffill. But intermediate rows are affected by bfill, and rolling indicators computed AFTER... wait, order: they clean OHLC first (ffill/bfill), then compute indicators from cleaned data. The indicator warmup NaNs are also ffill/bfill'ed. Since only the last row is consumed by decisions, and the last row's indicator values depend only on past data (trailing windows), bfill of early warmup rows doesn't leak future info into the last row. Hmm — but wait: EMA is recursive. EMA with adjust=False — pandas_ta's ema — the early values influence later values through recursion. If the first 20 EMA values are bfill'ed from the value at index 20, that changes the entire EMA series slightly, including the last value. Is that look-ahead? Strictly, yes — the EMA value at time t is influenced by data from time t+1...t+20 via the bfill seed. The effect decays exponentially and is negligible after 450 candles, but a scrupulous audit should note it. Compare: a "proper" approach would leave warmup NaN or seed from the first available value. The magnitude: EMA_20's dependence on the seed decays like (1-α)^n ≈ (19/21)^450 ≈ 0. Practically zero. I'll note it as a theoretical micro-leak with negligible practical effect — Minor, and arguably more relevant to future backtesting. Actually, for the panel's purposes, the practical effect is nil. But since Item 2 says "must NEVER influence," and backtesting is the stated next build, I should flag it as something to fix before backtesting. Minor severity for live; more important for the future backtester.

* Also, `data_fetcher` requests the latest `limit` candles from MEXC — no date-range parameter; fine for live.
* Determinism issue interacts with Item 2: live data changes between runs, so "same inputs" is hard to achieve in practice — that's Item 4/5.

So Item 2: Largely compliant for the live path, with the bfill seeding caveat and the fact that untested backtesting infrastructure doesn't exist yet. Status: Compliant with a minor caveat (or "Compliant — with one Minor observation"). The evidence is checkable: structure.py `_find_confirmed_swings` guard `if i + lookback >= n: continue`; indicators.py bfill calls.
Item 3 — Data Integrity.
Requirement: "Missing candles, duplicated candles, impossible prices, timestamp inconsistencies, NaN/Inf values, stale data, malformed API responses, and abnormal volume must be detected before they become analysis."
What exists:

* `fetch_ohlc`: checks response.raise_for_status, checks data is a non-empty list. ✓ malformed responses (partially).
* `_validate_dataframe`: checks None/empty, >= 20 rows, required columns present. ✓ (minimal)
* NaN/Inf: `add_technical_indicators` replaces inf→NaN, ffill/bfill. ✓ detection-and-repair.
* Missing candles (gaps in timestamps): NOT checked anywhere. No continuity check on the timestamp index.
* Duplicated candles: NOT checked (no df.index.duplicated() check).
* Impossible prices: NOT checked (no high>=low, no negative price/volume checks). volume_profile skips candles with high < low or volume < 0, but that's after indicators are computed — and silently ("skipped_candles").
* Timestamp inconsistencies: not checked (no monotonicity check).
* Stale data: NOT checked — no comparison of last candle timestamp vs current time. The engine will happily analyze a cached/stale response.
* Abnormal volume: zero/negative volume handled partially in volume_profile; no detection of volume spikes/drops.

So Item 3 is partially implemented: NaN/Inf and malformed-response handling exist; missing/duplicate/timestamp/stale/impossible-price detection does not. Verdict: Non-compliant (Major) — several enumerated failure classes have no detection. Evidence: absence is hard to "show," but I can point to data_fetcher.py (no gap/dup checks between receiving data and returning) and engine_core._validate_dataframe (only None/empty/length/columns). Someone can verify by grepping for "duplicated", "diff()", "monotonic", etc. — zero matches.
Note the A13 fix actually improved one part (fetch failure vs empty data distinction) — genuinely good. But Item 3's enumerated list is explicit, and most of it is unchecked.
Also `load_csv` does no validation at all, but it's not called anywhere in the pipeline. Still present as dead-ish code path.
Item 6 — Traceability.
Requirement: "Every major output must have an explainable lineage: decision ← decision components ← normalized signals ← raw signals ← indicators ← validated market data ← raw source data."
What exists:

* Decision Reasoning (explanation["reasons"]) — DecisionModel builds human-readable reasons. ✓ The C1 note says the reasons come from the same evaluation path.
* Entry quality sub-scores are exposed (ema_pos_pts etc.). ✓
* BUT: what is persisted? The panel is printed to stdout. The engine log (main.py's FileHandler) only logs "Starting/completed" plus errors. The panel text itself is NOT saved to the log (print() doesn't go through logging). The panel says "Trade logged to Logs/phase7_trade_log_{symbol}.csv" — but NO CODE writes that CSV! Let me check... Searching the source for "trade_log": panel_render.py mentions it in the footer string. Nothing else. engine_core doesn't write it. signal_router doesn't. live_trading writes to Logs/LiveSim/. So the panel claims a trade log CSV that nothing in the code actually writes. That's a fabricated provenance line — a direct traceability violation and arguably an epistemic-honesty violation (the panel asserts a fact that is false). Evidence: panel_render.py footer f"Trade logged to Logs/phase7_trade_log_{symbol.lower()}.csv"; no corresponding write anywhere (grep for "phase7_trade_log" or "to_csv" — only occurrence is the panel string). Also the evidence bundle does NOT include any such CSV — the package Section 4 lists only 4 evidence files and says "No other evidence exists to bundle." Corroborated by absence in the evidence bundle. This is a strong, checkable finding. Critical/Major? It's a Tier 1 invariant (traceability) — the lineage claim printed to the user is false. I'd call it Major (contained — it doesn't change decisions, but it falsely asserts a record exists). Actually the schema: Critical = "violates a Tier 1 invariant in a way that could reach a live decision or a customer." This does reach the operator (the "customer") — the panel literally tells the user a trade was logged when it wasn't. Hmm, but it doesn't reach a live decision. It's about records. I'd say Major, borderline Critical. Let me think: the failure mode — user believes there's an audit trail CSV; there isn't. For a decision-support tool whose Constitution elevates traceability to a Tier-1 invariant, a false claim of a persisted record is serious. I'll rate Major (it's contained: doesn't corrupt decisions) but note the argument for Critical. Actually, re-reading severity rubric: "Critical — violates a Tier 1 invariant in a way that could reach a live decision or a customer." The panel output reaches the customer. A false statement about logging reaches the customer. I think Critical is defensible; but the "live decision" is the trade decision. Hmm. I'll call it Major with a note that reasonable adjudication could rate it Critical. Let me keep consistent: I'll rate it Critical only if the false claim could induce misplaced trust affecting decisions. It could (user trusts there's a record; relies on it). I'll go Major — it's a provenance defect, not a decision defect. Hmm, actually let me reconsider: traceability Item 6's "why" is "If that walk breaks anywhere, the output can't be trusted." The walk from decision → raw data is NOT persisted anywhere: no decision object is written to disk at all. The only persisted artifacts: chart PNG, LiveSim JSON (only when test_live.py is run), state JSON (2 fields), engine log (INFO lines only). The full decision object with all sub-scores is printed to terminal and lost. So the walk-back "six months later" (Item 5's phrase) is impossible from artifacts. Both Items 5 and 6 fail on persistence.

Let me structure:

* Item 6 (Traceability): The in-memory lineage exists (decision object contains all components; explanation reasons exist; sub-scores exposed). The chain decision ← components ← signals ← indicators ← data is representable in the returned object. BUT: (a) nothing persists the decision object — the walk can only be done live, in the moment, from the terminal; (b) the panel's claim "Trade logged to ...csv" is false; (c) raw data is not persisted, so the final link (← validated market data ← raw source data) cannot be re-walked later. Verdict: Non-compliant (Major). Evidence: panel_render.py footer string; absence of any file write for the decision object (search: no `to_csv`, no json.dump of decision anywhere except LiveSim order which lacks most lineage); evidence bundle corroborates (no CSV, no decision dumps).

Wait — I should double check: does anything write the decision object? engine_core returns it; router returns it; main.py logs success/failure only. live_trading._log_simulated_trade writes the ORDER (a small projection) to Logs/LiveSim/ — but only when live_trading is run, and the order lacks bias/trend/structure/explanation. So yes, no full decision persistence.

* Also for Item 6: the chart saves to "Logs/Charts//chart..." (double slash — harmless). Fine.

Item 18 — Read-Only Market Access.
Requirement: never hold/request/have access to credentials with trade-execution permissions.
Evidence in front of me:

* The package's Search 1 (order-execution patterns): zero matches. I can verify against the provided source: scanning the 19 files, I see no order-placement calls. data_fetcher uses requests.get only. config.API_KEY/API_SECRET are empty strings. ccxt in requirements.txt is never imported (I can verify: imports across files — pandas, numpy, matplotlib, ccxt?, pandas_ta, requests, etc. — ccxt appears only in requirements.txt). ✓ I verified this independently by reading the provided source; I did NOT rerun the search, but the full text is in front of me and I can see no matches. I should note that my "verification" is reading the same text Claude searched — same artifacts, independent reading.
* HOWEVER: Item 18 says "The engine must never hold, request, or have access to API credentials carrying trade-execution permissions." The code has API_KEY/API_SECRET fields (empty), and the package claims public endpoints don't need keys. But: (a) the engine CAN be configured with keys via config.py — the fields exist, and nothing prevents pasting trade-enabled keys there; nothing in the code inspects or constrains key permissions (there's no code that checks the key is read-only); (b) ccxt is INSTALLED per requirements.txt — a library with full order execution capability is a declared dependency, importable by any future module. Item 18's rationale says the guarantee must live "in the exchange, where the engine cannot overrule it" — that guarantee is an operational property (what key the operator actually creates), not verifiable from source. From source I can verify: no order execution code exists; no credential is used in any request (requests.get with no auth headers — data_fetcher.fetch_ohlc passes only params symbol/interval/limit, no key). So the engine as-written doesn't use credentials at all.
* So what's the status? The code satisfies "never executes trades" structurally (no execution code paths). Whether the runtime environment holds a trade-capable key is not determinable from artifacts — the engine doesn't read env vars for keys (no os.environ usage for keys; config has empty strings). If keys are empty and unused, the engine effectively holds no credentials. But the config scaffolding INVITES keys ("Optional API keys (not required for public OHLC endpoints)"), and Item 18 says read-only "is the only permitted state" — with empty keys, no state exists. There's also no enforcement mechanism preventing a trade-enabled key from being added later (a process/control gap, arguably by design — the Constitution says execution capability would be "a different product").
* Careful: is there any hidden credential use? requests.get(url, params=params) — no auth. load_csv — local. No other network calls (search "requests." — only data_fetcher; "ccxt" — requirements only). OK.
* Status: Compliant for the code as written (no execution paths, no credential use), with an explicit note: the invariant's structural guarantee (exchange-side read-only permission) is not verifiable from source, and nothing enforces read-only-ness if keys are added later — that's an operational control per the Credential Security Protocol (not provided to me). The MVA gate's second-check requirement applies to this item per the DEFECT resolution. Also — requirements.txt includes ccxt unversioned and unused; an unused execution-capable dependency slightly weakens the "structural" claim; flag as Minor hardening observation. Also `API_KEY`/`API_SECRET` empty strings in config: they exist as fields; fine.

Hmm, wait — one more consideration for Item 18/1: `live_trading.py` — "NEVER executes real trades" — verified: it only builds dicts and writes JSON. ✓
DEFECT RESOLUTION (MVA gate composition):
The Constitution's Next Steps defines MVA as Items 2, 3, 6, 18. The Rev 3/6 safeguards text says "the Minimum Viable Audit gate items (Items 2, 3, 6) get checked a second time." These conflict on whether Item 18 gets the independent second check. The DEFECT row says "Pending that finding, the stricter reading governs: Item 18 is a gate item and does receive the independent second check. The audit must record a formal finding resolving which passage stands."
My finding: The gate definition passage (Next Steps, Rev 6, also Rev 1's original three-item gate expanded by Rev 6) is the authoritative definition — it's the later, deliberate, scope-changing revision that explicitly explains why Item 18 belongs ("if the running process holds a credential with trade permissions, then Item 1 is aspirational no matter what the code currently does"). The safeguards passage listing "(Items 2, 3, 6)" is a stale carry-over from Rev 3's original three-item gate (Gemini's MVA was 2,3,6 — see "What changed in Revision 1", item 2: "Items 2, 3, and 6 checked first"). When Rev 6 expanded the gate to four items, it updated the gate definition and Item 18's own "Minimum Viable Audit status" line, but missed the safeguards sentence. Resolution: the four-item gate stands; the safeguards passage is the defective text and should be read as covering all four gate items. This matches the DEFECT row's stricter interim reading. This is a finding against the Constitution document itself, not the engine. Record it formally. Also note: this document fix is blocked by the freeze (wording changes are allowed actually — "What isn't frozen is wording, phrasing, and explanatory material" — a correction that aligns wording with the already-ratified substance is arguably a wording fix, not a substance change; but the DEFECT row says the resolution "belongs to the auditor," and the document chose to record rather than repair. My finding resolves it; Viktor decides whether to issue a wording revision. Careful: I should resolve which passage STANDS, per the instruction. Resolution: Next Steps four-item definition stands; safeguards text should read "all gate items (2, 3, 6, 18)".)
Now the remaining Tier 1 items:
Item 1 — Tool, Not Autonomous Actor.

* No order execution code (verified above). ✓
* Position sizing: engine_core C4 BUILD computes position_size, position_value, risk_amount from config DEFAULT_ACCOUNT_BALANCE and DEFAULT_RISK_PERCENT and includes them in the risk dict — but panel_render.py comment says position size line was dropped from the panel per Viktor's request ("the underlying numbers ... are still computed upstream ... just no longer read/shown here as their own line"). Wait — is position_size shown? Panel text: I see "C4 BUILD: position size and the standalone EV line were both dropped from the panel per Viktor's request". The panel f-string indeed has no position size line. But live_trading's simulated order also doesn't include position size. So the engine COMPUTES position size but doesn't display it. Constitution Item 14 rationale (Rev 5): "never how much money is put on" — the engine does take risk tolerance as input per Entry #5 to shape entry/stop/target prices — hmm, does DEFAULT_RISK_PERCENT shape entry/stop/target? No — risk_percent only feeds calculate_position_size (display-only). The stop/targets are ATR-multiples, not risk-percent-shaped. That's fine — the Constitution allows risk% to shape prices but doesn't require it; it forbids money-amount recommendations. Computing position_size = risk_amount/stop_distance IS a money/quantity recommendation in money terms (units of asset for a $10k balance at 1% risk). Even though not displayed, it's computed and carried in the decision object — available to any consumer. Is that a violation of Item 1/Item 14? Item 1: "never independently execute trades or otherwise take irreversible external action" — computing a number isn't execution. Item 14's rationale: "It is not a reason for the engine to size positions or recommend how much to risk in money terms: that decision belongs to the trader alone." The engine DOES size a position (position_size, position_value, risk_amount in the risk dict), labeled "displayed for you to read." The panel hides it now, but the field exists in the returned decision object and any downstream consumer (e.g., a future UI) would see it. Tension with Item 14's "never how much money" clause. This is a real finding: Non-compliant (Moderate/Major?) with Item 14's money-terms clause — and arguably an Item 1 gray zone (no action, so Item 1 itself holds). Severity: the number is computed but not displayed; it could reach a consumer via the returned object (live_trading doesn't read it). I'd rate Moderate (Tier 1 tension, currently contained by not being displayed). Hmm — severity rubric: Moderate = "Tier 2 or 3 gap." A Tier 1 issue that's contained = Major. "Major — violates a Tier 1 invariant but is contained or not yet reachable." position_size IS reachable (in the returned dict) but not displayed. I'll say Major, contained. Actually wait — is Item 14's money clause an invariant requirement or rationale text? The invariant itself: "Directional conviction must never be treated as equivalent to risk." The rationale says the engine must not size positions. The rationale is interpretive but the Rev 5 correction was deliberate ("the engine does take the trader's chosen risk-tolerance percentage as an input ... and uses it to shape where entry, stop, and target prices sit — but how many dollars that percentage represents ... stays the trader's decision alone"). Computing risk_amount = balance × risk% = dollars at risk = exactly "how many dollars that percentage represents." So the engine violates the Rev-5-corrected boundary, albeit silently (not displayed). Finding: Non-compliant, Major (contained — not rendered). Also note EV line: decision_model computes EV using confidence as win-rate stand-in, described as "illustrative, not backtested," shown inside Decision Reasoning (per panel_render comment "EV ... still appears inside Decision Reasoning below" — wait, it says EV "still appears inside Decision Reasoning" — yes, _compute_ev appends to reasons). EV is presented as text in the reasoning trail with honest qualifiers. That's relevant to Item 7/8.
* Item 1 also: Exit Watch flags are advisory ("advisory only -- not automatic") ✓. The engine never takes external action. Simulated order logs only. ✓ Compliant on the core invariant.

Item 4 — Determinism.

* Sources of nondeterminism: (a) live data changes between runs (inherent to a live tool — same inputs clause can't be tested without fixed data); (b) indicator cache keyed on f"{symbol}{timeframe}{len(df)}_{df['close'].iloc[-1]}" — two different datasets with the same length and same last close collide → cache returns indicators computed on DIFFERENT data. That's a correctness bug and a determinism hazard: same key, different underlying data → outputs depend on cache state/history, i.e., prior runs affect current output (also Item 12 hidden state!). The cache key is not a hash of the data. Probability of collision in practice: last close changes tick to tick, so low but nonzero; also the same dataset re-fetched later with one new candle appended keeps same length? No — length would change. Same length + same last close happens when the latest candle updates in place (live candle) with same close but different high/low/volume — plausible. Then structure/indicators from stale cache. Also _manage_cache FIFO. This is a real defect: cache key insufficient. Severity: Moderate/Major. It can produce a panel computed on stale candles while claiming current data — that's an integrity/determinism problem that reaches the panel → I'd say Major (violates Item 4/12, can reach a live decision panel).
* (c) BiasStateMachine is instance state — within one process, repeated runs on same data give same results IF caches/state machines fresh; across a long-running process, state persists. In the normal invocation pattern (fresh process each run), BiasStateMachine starts NEUTRAL each time. But SignalRouter in live_trading is instantiated once at import (module-level singleton `live_trading_simulator = LiveTradingSimulator()`) — so repeated run_once() calls in one process share bias state machines and caches → run 2's output can depend on run 1 (hysteresis in _last_regime too — wait, StructureEngine is instantiated fresh inside calculate_structure each call, so _last_regime hysteresis is ALWAYS "NEUTRAL STRUCTURE" start — the hysteresis state machine is effectively dead: each call constructs a new StructureEngine, so _last_regime never persists across calls. The "state machine with hysteresis" is non-functional as designed — regime hysteresis can't work. That's a real finding: dead/broken feature. Evidence: structure.py calculate_structure creates `engine = StructureEngine()` per call; _detect_regime reads self._last_regime which is always initialized "NEUTRAL STRUCTURE". Impact: regime transitions always evaluated from NEUTRAL start; hysteresis never actually engages across runs. Minor-Moderate. Also means within a single analyze() call, _last_regime updates but is discarded.)
* (d) BiasStateMachine instances on Phase7Engine persist across runs WITHIN a process — detailed_bias depends on prior run's state. In main.py one run per process → deterministic given data. In live_trading singleton → cross-run contamination. Also BTC bias state machine.
* (e) numpy/pandas operations deterministic. No RNG anywhere (search "random" — none). No time-of-day logic except timestamps for filenames.
* Exception-clause rule: nondeterminism not documented → any nondeterminism is Non-compliant. The live-data dependence is inherent and obvious; but the cache-key and singleton-state issues are genuine findings.

Item 4 status: Non-compliant (Major) — cache key collision + stateful singleton in live_trading + non-functional hysteresis (also relevant to Item 12 and Item 16). Evidence: engine_core.py cache_key line; live_trading.py module-level singleton; structure.py calculate_structure instantiating StructureEngine per call.
Item 5 — Reproducibility.
"data timestamp, data source and version, engine version, configuration, and parameters must all be recoverable."

* engine_version exists in config but is never written to any output/log/decision object (search: engine_version defined in config.py; used anywhere? I don't see it referenced elsewhere).
* Decision object has symbol/timeframe but NO timestamp, no data-window info (first/last candle times), no config snapshot, no engine version.
* Raw data not archived (Constitution's Item 5 rationale explicitly says the audit should force the retention decision).
* The log has start/end timestamps per run — partial.
* Nothing records which parameters produced a given panel.
Verdict: Non-compliant (Major). Evidence: decision_object construction in engine_core.py (fields listed); absence of version/timestamp; no raw-data archive; the only persisted per-run artifacts are chart PNG and (for LiveSim) a thin order JSON.

Item 7 — No Unsupported Predictive Claims.

* The engine presents actions (LONG/SHORT/WAIT), confidence, EV, BTC-adjusted confidence. Are these "predictive"? The decision to go LONG is inherently predictive. Item 7 requires predictive components to be "empirically tested before presented as predictive."
* The Constitution itself flags Prediction V1 as "computationally validated, empirically unvalidated" and asks the label to be stated "wherever the feature is presented." Does the panel state it? The panel shows "BTC-ADJUSTED CONFIDENCE: x/100" with reasons text generated by decision_model: "BTC-adjusted confidence: ... (vs ... unadjusted, never replacing it)." The reason string does NOT say "empirically unvalidated." The EV reason says "Expected value (illustrative, not backtested)" — good honest qualifier. The BTC section header says "informational only -- does not change BIAS or DECISION above." But nowhere does the panel state the feature is "computationally validated, empirically unvalidated." The README (public repo) reportedly states it — but the requirement says "wherever the feature is presented," and the panel is the primary presentation surface. So the honest-status labeling is absent from the panel. Finding: Non-compliant (Major) — the specific remedy the Constitution prescribed (label on presentation) is not implemented in the panel. Evidence: decision_model._compute_btc_adjusted reason string; panel_render btc_section f-string — no "empirically unvalidated" text anywhere (search "unvalidated" → zero matches in source).
* Open Question 5.1: is the feature "additive, never distorts"? Verify: _compute_btc_adjusted returns separate dict; btc_context merge; panel shows it separately; decision/confidence untouched. Verified from code — TRUE that it's additive in code. But it IS presented as a "confidence" number (BTC-ADJUSTED CONFIDENCE: x/100) — a predictive-flavored score with no empirical validation, labeled only "informational only." So the open question resolves to: additive claim TRUE; epistemic labeling INCOMPLETE. The broader "confidence" itself (DecisionModel._compute_confidence) is a heuristic blend presented as "CONFIDENCE (decision): x/100" — per the audit working rule "confidence is not probability unless calibrated" — there's no calibration log; the EV calculation treats confidence as a win rate ("treating the confidence score as a rough win rate") while simultaneously disclaiming it. The EV display does say "illustrative, not backtested" — honest. But converting an uncalibrated heuristic into an EV number, even qualified, borders on manufacturing statistical meaning. The qualifier mitigates. I'll fold this into Items 7/8/9 findings.
* Also the whole engine presents LONG/SHORT decisions — is that an "unsupported predictive claim"? The engine presents decision-support, and Constitution Item 7 targets features "presented as predictive." The panel doesn't claim predictive accuracy. The DECISION line is a recommendation, inherently predictive in effect. Given the Constitution already scoped Item 7's application to Prediction V1 explicitly, I'll judge Item 7 on that basis plus labeling. Status: Non-compliant (Major) — prescribed label missing; evidence checkable by searching source for the label.

Item 8 — Epistemic Honesty.

* Positive: explanation reasons distinguish some things ("illustrative, not backtested" for EV; BTC "informational only"; error states distinct from HOLD — A13 fix; risk gate vs validation disambiguation comment in decision_model).
* Negative: (a) "confidence" presented as x/100 with two decimals (panel prints .2f) without stating it's an uncalibrated heuristic — though decision_model's reason describes its composition; (b) the false "Trade logged to ...csv" line is an epistemic-honesty violation (asserts a fact that is false) — already under Item 6; (c) scan report evidence shows VALIDATION: STRONG (Score: 45.00) — old build artifact, but indicates the current code fixed the display; current code shows validation_score. OK. (d) The panel's "CONFIDENCE (decision)" now labeled "(decision)" — decent. (e) trend_direction "Informational only" comment says doesn't feed decision logic — but it DOES feed entry_model multipliers (trend_multiplier) — the comment in trend_health says "Informational only -- doesn't feed into or change trend_health, bias, or any decision logic" while entry_model uses trend_direction to adjust entry score which feeds DecisionModel via entry_score (trend_health >= 75 and entry_score >= 70 gate). So the comment is false — trend_direction DOES influence the decision through entry quality multipliers. That's a provenance/honesty defect in documentation, and also relevant to Item 11/12. Check: entry_model.calculate_entry_quality takes trend_direction, applies trend_multiplier (1.05/0.90) → total_score → entry["score"] → decision gate. Yes. And the Constitution's own "Already known — Layer 5" item asks whether macro_bias, trend_direction, structure_sequence were partly derived from each other upstream (circularity) — separate question, Item 11.
* Status: Non-compliant (Moderate/Major). The most concrete checkable evidence: (1) missing "empirically unvalidated" presentation label (overlaps Item 7); (2) false trade-log claim (overlaps Item 6); (3) trend_health.py docstring claiming trend_direction doesn't affect decisions while it does. I'll record Item 8 as Non-compliant (Major) with the three evidence points, cross-referencing.

Item 9 — Every Measurement Has a Precise Definition.

* "confidence": defined in decision_model._compute_confidence — composition stated in code and in reasons text (bias_strength0.5 + trend_health0.3 + alignment + validation_adj). Precise enough as a heuristic definition. ✓ mostly.
* "trend_health": defined in trend_health.py (slope 45 + ADX 40 + RSI 15). ✓ documented in code.
* "entry quality": defined with sub-scores (30/25/20/15/12 = 102 max? 30+25+20+15+12 = 102 — hmm, docstring says "Max points: 100" but components sum to 102; total capped at 100. Minor inconsistency; also multipliers can exceed 100 → capped. The docstring says max 100 while parts sum 102 — sloppy but bounded.)
* "risk": risk_score = bias_score aliased (engine_core sets "risk_score": bias_score) — semantic aliasing: risk_score contains bias_score. Same field-name confusion the Constitution warns about (Item 10). Also "signal_strength": bias_score — another alias. confidence_score (engine_core sets = trend_health, then router OVERWRITES with DecisionModel confidence) — engine_core's risk dict sets "confidence_score": trend["trend_health"] and signal_router replaces it with real confidence. The double-assignment is confusing but the final value is the router's. Note panel comment said "risk_score stays for CONFIDENCE below, which was already reading the right field" — wait, panel reads confidence_score for CONFIDENCE line. And risk_score is... not displayed? The panel doesn't show risk_score after the fix (VALIDATION uses validation_score). risk_score and signal_strength exist in the dict but aren't displayed. OK.
* "bias_score": -100..100 weighted blend, documented. ✓
* "validation_score": ad hoc — trend_health + 5/-5 macro + 10/-15 volume, clamped — defined in code, semantic meaning fuzzy ("validation" of what?). It exists; definition is in code.
* Overall: most terms have code-level definitions; there's no term registry (Engineering Notes Entry #6 pending), and the audit-time scoping decision (measurement vs derived vs heuristic vs interpretation) hasn't been made. The Constitution says that distinction gets resolved during the audit under Item 8. So my audit should make that call: confidence/trend_health/entry_quality/validation_score are HEURISTIC SCORES, not measurements; bias_score is a derived quantity/heuristic blend; correlation/beta are measurements/derived quantities; ATR/RSI/EMA are derived quantities with standard definitions. Panel renders heuristics and measurements with the same visual weight (x/100 vs prices) — relevant to the Lens-vs-Property candidate (not adopted; informational).
* Status: Compliant with Minor gaps — or Unknown? The invariant requires explicit mathematical or semantic definitions. Code-level formulas exist for all emitted scores (checkable). The 30+25+20+15+12=102 vs "Max 100" docstring inconsistency is Minor. The "risk_score"/"signal_strength" aliasing is an Item 10 problem more than Item 9. I'll say Compliant (Minor observations). Hmm — is there any emitted number with NO definition? "swing_struct" — defined (nearest confirmed swing). "distance_from_zone" — defined. "volatility mode" thresholds — defined in code (0.01/0.02/0.04) but "NORMAL" default vs "LOW VOLATILITY" — validate_risk_parameters default volatility_state="NORMAL" but engine now passes real volatility_mode ✓. calculate_stop_targets default "NORMAL" — engine_core calls it WITHOUT volatility_state! Check: engine_core calls self.risk_model.calculate_stop_targets(detailed_bias, trend_health, current_price, atr_val, structural_level=hvn, bias_score=bias_score) — NO volatility_state argument! So vol_multiplier is always 1.0 ("NORMAL") in stop/target calculation, even though the A-fix restored EXTREME tier and validate_risk_parameters receives real volatility_state. The documented fix in bias_engine.py says "risk_model.py has a real EXTREME VOLATILITY tier (widest stops, halved position sizing...)" — position sizing does get volatility_state (engine passes volatility_state=volatility_mode to calculate_position_size ✓) but stops/targets DON'T. So "widest stops in extreme volatility" never actually happens — dead parameter at the call site. Real defect: engine_core.py call omits volatility_state. Evidence: engine_core.py section 8 call vs risk_model.calculate_stop_targets signature. Impact: stops not widened in high/extreme vol — risk geometry less conservative than designed. Major? It affects stop/target prices shown on panel → reaches decisions → I'll fold under Item 9? No — this is a wiring bug; belongs under... it's a correctness defect relevant to Item 14 (risk geometry) and Tier 2 (explicit interfaces/evaluated changes). I'll record it under Item 14 and reference in Tier 2. Actually — better under Item 10/semantics? No. It's a parameter-not-passed bug. Tier 2 "explicit interfaces/contracts" or Item 16? I'll put it under Item 14 (Risk Is Not Conviction — risk geometry) as the most decision-relevant home, and cross-ref Tier 2 evaluated-changes.
* So Item 9: Compliant (Minor) with noted inconsistencies.

Item 10 — Consistent Semantics.

* "confidence_score": engine_core sets risk["confidence_score"] = trend["trend_health"]; signal_router overwrites with DecisionModel confidence. Within one returned object the final value is consistent. But engine_core's decision_object (when router not used... always used via router) — main.py uses router. OK.
* "risk_score" = bias_score; "signal_strength" = bias_score: two names, same value, neither displayed. Aliasing debt — not user-visible confusion currently, but the field names claim different things. Minor.
* "trend.health" vs "trend.trend_health" — router sets both keys to the same value (compatibility duplication). Harmless but smelly.
* "VALIDATION" — fixed per comment (was displaying risk_score). ✓ fixed.
* "MACRO TREND" bullish counts +5 in validation_score regardless of trade direction? val_score adds +5 if macro BULLISH even if the contemplated trade is SHORT — validation doesn't know direction. Minor semantic wobble; validation_state feeds confidence (validation_adj) and the WAIT gate ("validation_state == WEAK and trend_health < 40"). So a bullish macro boosts validation even for a short decision. Not displayed prominently. Minor-Moderate. Hmm — but is it semantically wrong? "validation" is direction-agnostic in design; macro agreement should depend on direction. It inflates confidence for shorts in bull macro. I'd flag Minor.
* "Action" semantics: exit.action now carries DecisionModel final_action (LONG/WAIT/etc.) while compute_exit returns "final_action": "HOLD"/"STOP LOSS HIT" — router discards exit's final_action in favor of DecisionModel's (exit: {"action": final_action}). compute_exit's own exit_signal (HOLD/TARGET HIT) is computed from long/short signals and current price vs stop/targets, then thrown away. Dead computation — compute_exit's exit conditions never surface. Also within exit_model, "final_action" name collides conceptually with DecisionModel's final_action (different meanings: position-exit event vs trade decision). Item 10 semantic collision — same phrase "final_action" used for two different things in two modules; the old live_trading bug comment even documented "exit.final_action -> exit.action" rename. Flag Moderate (latent confusion, currently contained because exit_data["final_action"] is never read downstream).
* The historical confidence-score bug (Constitution's own example) — fixed per roadmap Layer 1.
* Status: Non-compliant (Moderate) — the "final_action" term has two meanings across modules; risk_score/signal_strength alias bias_score under different names; validation_score direction-blindness. Mostly contained. I'll say Moderate (Tier 1, contained → technically "Major" per rubric...). Rubric: Major = violates Tier 1 but contained/not reachable. The final_action collision: is it reachable? exit_model's final_action is discarded; not reachable to panel. So contained → Major? Hmm, "contained or not yet reachable" → Major. But the practical impact is low. The rubric forces Major for any contained Tier 1 violation. Let me re-read: "Moderate — a Tier 2 or 3 gap..." So any Tier 1 violation is at least Major. OK — then several of my "Minor" Tier-1 observations need re-rating: for Tier 1 items, severity is Critical (reachable) or Major (contained). Minor is for cosmetic/Tier 4. I'll rate accordingly: contained Tier-1 violations = Major; theoretical/negligible ones I can call Minor with justification? The rubric says Minor = "cosmetic, cleanup, or a Tier 4 preference." Strictly, then, any real Tier-1 violation is Major+. I'll use Major for contained ones and note impact honestly in Impact field.

Let me reconsider severity assignments under this rubric:

* Item 3 (data integrity gaps): missing-candle/stale-data detection absent — can reach decisions (engine analyzes incomplete data confidently) → Critical? "violates a Tier 1 invariant in a way that could reach a live decision or a customer" — yes: a gap in 4h candles would silently shift indicators and the decision. The panel gives no warning. I'd rate Critical... but is it "could reach" — yes, bad data flows directly into bias/decision. Critical. Hmm, but NaN/Inf and malformed-response handling DO exist; the missing classes are missing-gap/duplicate/stale/impossible-price/abnormal-volume detection. A stale dataset producing a confident panel is exactly Item 3's enumerated fear. I'll rate Major-Critical; decide: Critical for the combination of no staleness/gap detection, since the panel renders full confidence regardless. Actually let me not over-inflate: the engine fetches fresh data each run from the API; a stale response is unlikely from a live API but possible (caching proxies, API returning old data). Missing candles on MEXC klines — possible (exchange gaps). No detection exists. I'll say Major — real, reachable in principle, but requires an external data fault to manifest. Hmm, rubric says Critical if it COULD reach a live decision — it could. But everything "could." I'll use judgment: Critical reserved for clear-and-present; Major for contingent. Missing-gap detection: contingent on exchange data fault → Major. Fine.
* Item 6 false trade-log line: reaches the customer (asserts false record) → rubric-wise Critical ("could reach ... a customer"). It DOES reach the customer every run. But it's a provenance falsehood, not a decision corruption. Rubric literal reading: Critical. I'll rate Major with explanation? Or Critical with explanation? The schema says severity independent of status; an honest auditor applies the rubric: the false claim reaches the customer every single run → Critical seems right literally, but the spirit of Critical is decision-impacting. The rubric's "or a customer" clause suggests they intended things like UI falsehoods to count. A false claim that an audit trail exists directly undermines the audit-trail invariant — for a Constitution whose entire point is trustworthy artifacts, I'll rate it Major, noting that if Viktor reads the rubric strictly, it qualifies as Critical because it reaches the customer on every run. Transparency over precision. Hmm, I need to just decide. Let me decide Major, with a note that a strict rubric reading supports Critical. Actually — simpler: I'll call it Critical. Reasoning: it is a false statement of fact presented to the operator on every run, directly contradicting a Tier 1 invariant (traceability), and it could plausibly cause the operator to rely on a nonexistent record. The rubric explicitly includes "or a customer." Decision: Critical. Hmm, but then Item 5/6 persistence gaps... those are absence-of-capability rather than false claims. Let me settle:
   * False "Trade logged" claim: Critical (false provenance presented to operator every run).
   * No decision-object persistence / Item 5 gaps: Major.
* Item 18: Compliant (as code), with unverifiable operational half (Unknown on the exchange-side permission state — but the invariant as written governs the engine's holding/requesting credentials; the engine holds none per artifacts; the empty config fields are verifiable. The key's exchange-side permission can't be verified from artifacts — no key exists. So Compliant, with note.)

Wait, careful on Item 18: "Read-only access is not a default setting — it is the only permitted state." With API_KEY = "" and no auth used, the engine has NO access beyond public endpoints. Public unauthenticated access is even weaker than read-only keys. So the engine never holds trade-execution credentials — Compliant from code. Caveat: config scaffolding invites keys; ccxt dependency present unused; neither violates the letter today. Compliant. Also note: no code path prevents a future dev from adding keys+ccxt orders — that's what change control is for (Tier 3), not Item 18 itself.

* Item 2: Compliant with a Minor (negligible) bfill-seed note; and the swing-confirmation design is genuinely correct. I'll mark Compliant, note the bfill caveat as a Minor observation for the future backtester.

Item 11 — No Circular Reasoning. This is the Layer 5 open question (Constitution's "Already known — Layer 5: Unknown").
The question: entry quality's multipliers combine macro_bias, trend_direction, structure_sequence — are any of these derived from each other upstream?
Let me trace derivations:

* macro_bias: from df_macro (1d data): close vs EMA_50 on the daily. Derived from macro timeframe price data only.
* trend_direction (trend_health.py): sign of trend_slope = avg of EMA20_Slope and EMA50_Slope on the 4h execution data. Derived from 4h EMAs.
* structure_sequence (structure.py B2): swing pivots on 4h high/low. Derived from 4h price extremes.
* entry quality base score: EMA zone position (4h EMA20/50), ATR distance, VWMA distance, RSI, HVN proximity.
* The three multipliers: macro (daily EMA50 relation), trend_direction (4h EMA slopes), structure_sequence (4h swing breaks).

Are these "the same signal in different accents"?

* trend_direction derives from EMA20/EMA50 slopes; the entry zone IS EMA20/EMA50, and ema_pos_pts measures price proximity to that zone. So trend_direction and ema_pos_pts share the same underlying EMAs — partially correlated but measuring different aspects (slope sign vs distance). Is that "reinforcing through multiple derived layers presented as independent confirmation"? They both stem from the same EMA pair. The entry-quality docstring presents them as separate factors (base + multiplier). Partial overlap, not full circularity.
* structure_sequence (swings) and struct_pts (HVN proximity): different data (pivot extremes vs volume profile) — mostly independent.
* macro_bias (daily) vs 4h trend: correlated in trending markets but different timeframe/data — the Constitution treats MTF confluence as legitimate.
* Bigger circularity candidate: bias_score already includes structure_regime, volume_sentiment, supertrend, macro_bias, trend_health. confidence = 0.5*|bias_score| + 0.3*trend_health + structure_alignment(bias vs structure regime) + validation_adj. trend_health appears in bias_score (30% weight) AND directly in confidence (30%) AND structure_regime appears in bias_score (20%) AND in structure_alignment bonus, AND validation_score = trend_health + macro + volume (all three also in bias_score). So confidence double/triple-counts the same inputs: trend_health enters confidence directly (0.3) and via bias_strength (0.5 × 30% = 0.15) and via validation (trend_health base) → effectively ~45%+ weight on trend_health, presented as a fresh "how much the picture agrees with itself" number. Similarly structure and volume counted twice (bias blend + alignment/validation). This IS the Item 11 pattern: the same signals reinforcing through multiple derived layers and presented as independent confirmation ("bias strength is X, trend health is Y, structure agrees..."). The decision gate also re-uses trend_health (>= 75) and entry_score.
* Also generate_entry_signals uses detailed_bias (from bias blend incl. structure/macro) AND structure_regime AND macro_bias again → gating double-counts.
* The specific Layer 5 question (macro_bias, trend_direction, structure_sequence derived from each other?): Direct answer: NO — they are computed from distinct upstream series (daily EMA50 vs 4h EMA slopes vs 4h swing pivots). None is derived from another. So the specific hypothesis the Constitution floated is NOT confirmed — but a DIFFERENT circularity exists in confidence construction (trend_health/structure/volume triple-counted between bias_score, confidence, validation_score). I must report the open question tested: no upstream cross-derivation among the three multiplier inputs; however trend_direction shares underlying EMA data with the entry zone (partial overlap), and separately, confidence exhibits multi-layer reuse of the same factors. So Item 11: Non-compliant (Major) on the confidence/validation construction, while explicitly recording that the specific Layer 5 hypothesis (cross-derivation of macro_bias/trend_direction/structure_sequence) tested FALSE as stated. This is exactly the "test, don't confirm" posture.

Evidence checkable: decision_model._compute_confidence formula; engine_core validation_score block; bias_engine weights; entry_model multipliers; trend_health trend_direction derivation.
Item 12 — No Hidden Decision-Affecting State.

* Documented state: (a) _indicator_cache/_structure_cache in Phase7Engine (in-memory, FIFO-bounded) — hidden in the sense of undocumented? It IS commented ("Bounded cache sizes...") in code. Documentation "before the audit began": code comments exist. But the cache key flaw (len + last close) means state can AFFECT decisions wrongly. Caching is decision-affecting state; it's documented in code comments; but the key-collision behavior is an undocumented hazard → defect. (b) BiasStateMachine state — within-run only in main.py pattern; persists across run_once calls in the LiveSim singleton — that cross-run influence is NOT documented (live_trading docstring doesn't mention state carryover). (c) phase7_state JSON file — explicitly documented (C3 comments) for Exit Watch comparisons. ✓ documented. (d) StructureEngine._last_regime — intended hysteresis state, but dead (fresh instance each call) — documented intent, non-functional implementation.
* The cache-key collision = same inputs (new fetch) can yield different outputs depending on cache contents → hidden state affecting decisions, undocumented behavior. Non-compliant (Major). Evidence: engine_core cache_key construction; _manage_cache; live_trading singleton instantiation at import.
* Note the evidence file phase7_state_AEROUSDT_4h.json contains {"supertrend_direction": 1.0, "detailed_bias": "BULLISH CONFIRMED"} — consistent with C3 documented state. ✓ (matches _save_state fields exactly — good corroboration that the evidence files are genuine).

Item 13 — Fail Safely.

* Positive: A13 fetch-error distinction (verified in data_fetcher/engine_core: fetch_error attrs → distinct error message); risk gate blocks trades (NO-TRADE) on invalid risk; decision defaults WAIT on malformed inputs; error states render as [ERROR] not as decisions; BTC context failure → "unavailable" without breaking AERO analysis (verified try/except in engine_core 6b and _merge_btc_context requiring available on both).
* Negative: (a) indicator fallbacks INVENT plausible values when computation fails — e.g., RSI falls back to 50.0 constant, ADX to 25.0 constant, SuperTrend to close/1.0, ATR to close*0.02, KAMA→EMA. clean_series fills NaN with median/0. These silent fabrications produce a confident-looking panel from broken indicator computations — exactly what Item 13 forbids ("never invent a confident-looking answer"). The fallbacks are silent: no flag in the decision object says "RSI was fabricated." The engine continues with neutral-looking numbers rather than degrading confidence or halting. This is a direct Item 13 violation pattern, though motivated by robustness. Severity: the fabricated-neutral values bias toward NEUTRAL (RSI 50, ADX 25) — arguably "becoming less confident" in effect (neutral values → lower scores)? A constant RSI=50 doesn't lower confidence; it pretends normalcy. The failure isn't surfaced. Evidence: indicators.py fallback blocks ("except Exception: df['ADX'] = pd.Series(25.0...)"). Also trend_health exception → default_response (50.0 etc.) with no surfacing; engine_core catches trend-health failure and substitutes 50.0 defaults silently (logs error but decision object carries no flag). Also structure sub-routine exceptions fall back to last regime/current price silently. Panel shows nothing. So partial compliance: data-fetch failures handled well (A13); computation failures masked by silent defaults. Non-compliant (Major).
* Also _validate_dataframe requires ≥20 rows; indicators like EMA_50 need 50; with 20-49 rows, EMA_50 fallback seeds... ffill/bfill fabricate. The 20-row minimum is below indicator warmup needs — a data-adequacy gap (Item 3/13 overlap). Note it.

Item 14 — Risk Is Not Conviction.

* Positive: risk gate independent of confidence: risk_valid from validate_risk_parameters can block (NO-TRADE) regardless of confidence; decision_model adds qualifier when NO-TRADE ("not a green light"); stop geometry ATR-based, volatility-tiered in sizing; the sim trade evidence shows exactly this working: long_signal true + risk_valid false → decision NO-TRADE (RISK TOO HIGH). ✓ That record is good evidence the gate fires in practice.
* Negative: (a) volatility_state never passed to calculate_stop_targets (call-site omission) → stops never widen in HIGH/EXTREME vol — the documented protection is inert; (b) confidence reuses trend_health heavily (Item 11) but that's conviction-side; (c) position_size computed (money terms) — the Rev 5 boundary issue (recorded under Item 1/14 above); (d) EV display uses confidence as win rate — blends conviction into value terms, albeit qualified. (e) bias_factor = 1 - |bias_score|/300: higher conviction NARROWS the stop (stop_mult smaller → tighter stop for LONG? stop_mult multiplies atr distance: higher bias_score → smaller stop_mult → tighter stop). Wait: bias_factor = 1.0 - abs(bias_score)/300.0 → higher conviction → factor <1 → stop_mult reduced → stop CLOSER to price. That's literally conviction reducing risk allowance — the exact substitution Item 14 forbids! Being more confident makes the stop tighter. Let me double check: stop_mult = atr_stop_mult * trend_factor * bias_factor * vol_multiplier. atr_stop for LONG = price - atr*stop_mult. Higher |bias_score| → smaller stop_mult → tighter stop. Yes: directional conviction directly tightens risk distance. That is a textbook Item 14 violation embedded in the formula: conviction treated as risk reducer. Severity: it directly shapes the STOP LOSS and targets shown and used in risk validation → reaches live decision support → Critical. Evidence: risk_model.py bias_factor line. Counter-consideration: maybe intended as "stronger bias → more confident → tighter stop OK"? The Constitution forbids exactly this reasoning. Non-compliant (Critical).
* Also trend_factor = 1 + trend_health/200 — higher trend health WIDENS stops (up to 1.5×) — that's conviction INCREASING risk allowance (wider stop = more risk) — direction-wise that's "confident → take more risk," again conviction↔risk coupling, opposite sign. Both factors couple conviction to risk geometry. The Item 14 violation stands regardless of sign: conviction metrics directly scale the stop distance.

Item 15 — Empirical Evidence Supersedes Theoretical Expectation.

* There is no empirical testing infrastructure at all (no backtester — removed; no validation logs). Item 15 governs conflicts between measured behavior and theory; with no measurement apparatus, the invariant is dormant rather than violated. The engine's parameters (weights, thresholds) have no empirical validation — but Item 15 only bites when measurement exists. Status: Unknown (no evidence either way; no empirical harness exists to invoke it). The Constitution defers backtesting deliberately. Note: Item 7's required empirical validation also blocked → noted there.

Item 16 — Complexity Must Provide Demonstrated Value.

* Evidence of value demonstration: none in artifacts — no tests, no measurements for any layer. Comments claim fixes (A1-A13, B1, B2, C1, C3, C4) but no measured before/after. Dead/broken complexity exists: (a) hysteresis state machine non-functional (fresh instance each call); (b) compute_exit's exit-condition logic computed then discarded (router overrides with DecisionModel action) — dead code path; (c) KAMA computed, used only for KAMA_Slope which is... used anywhere? search KAMA_Slope usage: indicators.py computes; nothing reads it (trend_health uses EMA slopes only). Dead indicator. (d) BB bands computed — used anywhere? search BB_ : nothing reads BB_upper/lower/middle outside indicators.py/plotting? plotting uses VWAP (never computed! plotting checks "VWAP" column which is never created — dead chart element) and EMAs. BB unused. DIP/DIM — used anywhere? No reads. Dead. (e) Typical_Price — used anywhere? No. Dead. (f) load_csv — never called. Dead. (g) pct_slope function — defined, never called (inline slope calc used instead). Dead. (h) live_trading's engine_result passthrough fine. (i) requirements ccxt unused. So substantial dead/undemonstrated complexity. Item 16 says every layer "should be able to point to the specific problem it solved; if it can't, that's a finding." → Non-compliant (Major, contained) — dead code doesn't corrupt decisions but violates the invariant and adds collision risk (the deleted supertrend.py comment shows they know). Severity per rubric: Tier 1 contained → Major. Impact: maintenance hazard, semantic collision risk, dead paths imply untested code. Moderate-Major; rubric: Major.

Item 17 — Backtesting Isolated From Core Engine.

* No backtesting code exists (package confirms removal; public README status). The invariant's containment requirement applies when backtesting exists. Currently: vacuously satisfied / not invocable. Status: Compliant (vacuous) — or Unknown? There's nothing to violate; the precondition (backtesting exists) is false. I'd record Compliant-by-absence with note that the real test comes when the backtester is built; the Constitution already sequences it after the audit. I'll mark "Compliant (vacuously — no backtesting code present)" and flag Re-audit: yes, mandatory when backtesting build starts.

Item 19 — Withdrawal Permissions Never Enabled.

* No credentials at all; no withdrawal-capable code; no transfer endpoints. Compliant (code-level). Same caveat re exchange-side key config — no keys exist. Compliant.

Item 20 — Credentials Never Exposed.

* Hardcoded: API_KEY/API_SECRET empty strings in config.py — no real credentials hardcoded (verified in provided source).
* Logs: check the provided engine log — no keys (only INFO/ERROR lines). Decision object: no credential fields. Error messages: str(e) from exceptions could contain... requests errors could include URL (contains only symbol/interval/limit params — no auth params, so no key leakage even if keys were set, because keys are never sent). Stack traces printed (traceback.print_exc()) — no credentials in scope.
* One risk: if API_KEY were set, nothing sends it, so nothing logs it. Compliant (code-level). The public-repo history is out of scope of artifacts (can't verify git history; package claims evidence files gitignored — I cannot verify .gitignore content; Unknown on repo-history exposure, but nothing in the provided artifacts shows a real key). I'll mark Compliant on artifacts, note the git-history aspect as unverifiable from this package.

Item 21 — Operator Credentials Stay With the Operator.

* No telemetry/crash-report/upload code exists (search: no requests.post, no sockets, no analytics). Single-user local tool currently. Compliant (vacuous on multi-user; code contains no conveyance channels). ✓

Tier 2 (7 principles):

1. Separation of responsibilities: data_fetcher only fetches ✓; indicators compute ✓; decision in DecisionModel ✓ (router purified per docstring — verified: router calls decision_model.evaluate; no decision logic remains in router ✓). BUT: engine_core computes validation_score/validation_state inline (a decision-ish scoring inside the orchestrator, not the decision model) and risk dict carries aliases; engine_core also computes macro_bias inline. Mostly separated; validation logic arguably misplaced in orchestrator. Panel_render reads decision object only ✓. Status: Largely Compliant; note validation_score living in engine_core as a Moderate/Minor architectural observation.
2. Observation → interpretation → decision stages: EMA20>EMA50 (observation, in macro bias) → labels like "BULLISH TREND" (interpretation) → DecisionModel (decision) → risk. Stages exist and are distinguishable; entry/risk shaped downstream. Some blurring: bias_engine mixes raw observations and interpretations in one blend (acceptable as a defined function); the panel labels things with interpretation names. I'd say Compliant.
3. Explicit interfaces/contracts: StructureAnalysisResult TypedDict exists for structure (nice). Decision object shape is implicit but consistently consumed; function signatures typed in places. The documented drift bugs (live_trading field renames, "bogus reference_price kwarg silently absorbed by **kwargs") show contracts were NOT explicit historically; **kwargs in validate_risk_parameters remains (silently absorbs wrong kwargs — the A6 comment documents this caused a real bug; the **kwargs is STILL there: "def validate_risk_parameters(..., **kwargs)" — still absorbs anything silently). That's a live contract weakness: the very mechanism that caused bug A6 remains in place. Finding: Non-compliant (Moderate) — evidence: risk_model.validate_risk_parameters signature retains **kwargs; historical comments document multiple contract drift breakages. Also entry_model signature changes rippled (engine_core had to add args). Moderate.
4. Explicit configuration: config.py centralizes many (EMA, RSI, ATR lengths, supertrend, risk %, balance ✓). BUT: many magic numbers buried: bias weights (0.30/0.20/... in bias_engine module constants — named constants, acceptable), thresholds RAW_BIAS_THRESHOLD=20, BiasStateMachine 30/20, trend_health component caps (45/40/15, tanh scalings), entry_model point budgets & multipliers (1.05/0.90), validation +5/-5/+10/-15, confidence blend 0.5/0.3, BTC cap 20/penalty 15, decision gates 75/70/40, risk model multipliers as instance attrs (1.2/1.0/2.0/3.0 — configurable-ish), vol thresholds 0.01/0.02/0.04 in bias_engine, stop validation 15%/0.2%. The Tier-2 rule says "Important behavior comes from named, visible configuration — not mystery constants buried in the logic." Many ARE named constants at module level (bias weights) but NOT in config.py and not visible to an operator; others are bare literals inline (trend_health rsi bands, decision gates). Status: Non-compliant (Moderate) — significant behavioral constants live outside config. Minor-Moderate; it's Tier 2 → Moderate per rubric.
5. Modular design: one job per module mostly holds; engine_core is a 500-line orchestrator doing validation scoring — borderline; modules have single responsibilities otherwise. Compliant with Minor note.
6. Controlled dependencies: dependency map: main→router→engine_core→{data, indicators, structure, models}; live_trading→router. No circular imports evident. models don't import engine_core. Reasonably intentional. The log shows one historical circular-ish import error ("cannot import name 'config' from 'models'") during dev — fixed. Compliant (Minor note).
7. Explicit, evaluated changes to interfaces or behavior: The code comments document evaluated changes (A1-A13 etc.) — but the same comments document repeated silent-breakage history (14-module rename in Constitution; live_trading drift; A6 kwargs absorption). No tests exist (no test files in the 19; test_live.py is a smoke runner, not an assertion). Without tests, "evaluated before acceptance" can't be evidenced. Status: Non-compliant (Moderate) — no test harness exists to evaluate downstream effects; history shows repeated contract drift. Evidence: absence of any test/assert files in manifest; test_live.py contains no assertions.

Tier 3 (10 process items):

1. Hypothesis-driven development: Cannot verify process from artifacts. Code comments reference a "fix plan" (A/B/C numbering) suggesting structured work; but no hypothesis/test/measure records in package. Unknown.
2. Controlled changes: Same — Unknown from artifacts (comments suggest labeled fix passes A1..A13, B1/B2, C1/C3/C4 — some evidence of grouping; can't verify one-change-at-a-time). Lean: partial evidence via systematic labels → still Unknown formally? The labels suggest batch passes ("A3/A4/A5 pass") — groups. I'll say Unknown.
3. Automated tests: None exist. Non-compliant (Moderate). Evidence: manifest has no test files; test_live.py asserts nothing; requirements has no pytest.
4. Regression tests: None. Non-compliant (Moderate). Same evidence.
5. Fixed evaluation datasets: None (load_csv exists but no dataset files, no eval harness). Non-compliant (Moderate).
6. Version control: Public GitHub repo exists (package states; I can't browse — no web tool needed? I do have web tools! I could verify the repo exists. But is that necessary? The claim "github.com/CaroluxRex1987/phase7-engine" — I could fetch. Let me consider whether to use web_fetch. The audit says evidence must be independently checkable; the repo's existence supports Tier 3 version control. I can attempt a fetch. Also note .gitignore claim unverifiable from artifacts — fetching the repo could confirm file list but not history depth. Let me try fetching the repo URL to verify existence. It's optional; if unavailable, mark accordingly.)
Actually — careful with web fetch: the environment may or may not have internet. I'll attempt one fetch of the GitHub repo. If it fails, note Unknown for repo existence (the package's claim unverified), though source presence itself doesn't prove version control use (could be uploaded once). Version control = "real history, not zipped backups" — a git repo URL is claimed; even if it exists, commit history depth is unverified. I'll attempt fetch; else Unknown.
7. Known-good checkpoints: No evidence (no tags/releases mentioned; can't verify). Unknown. The engine log shows repeated breakage between runs (syntax errors etc. during dev) — development practice evidence is thin. Unknown.
8. Reproducible experiments: No experiments/harness; live-data dependence makes reruns non-reproducible (Item 5 failure compounds). Non-compliant (Moderate).
9. Rollback capability: If git repo exists with history, partially; can't verify → Unknown.
10. Documentation of significant decisions: Code comments are extensive and dated-by-label (A13, B2, C4) — genuine decision documentation inline; Engineering Notes referenced but not provided. Inline comments do record why (e.g., A12 rationale). Partially Compliant — I'd say Compliant (with the caveat that comments are unversioned rationale; an ADR log is only a future candidate). Evidence: the fix comments themselves. Moderate→Compliant. I'll say Compliant, noting no external decision log in artifacts.

Tier 4 (6 preferences): preferences — assess briefly, leniently:

1. Robustness over optimization: heavy defensive coding/fallbacks — but the fallbacks themselves mask failures (Item 13 tension). Mixed → Minor note (preference, no severity teeth).
2. Generalization over historical fit: engine is symbol-generic (works on BTC/ETH/etc. per scan report) ✓ leaning compliant; parameters tuned by hand, no fitting evidence. Fine.
3. Explainability over opacity: explanation reasons, sub-scores, plain-language panel ✓ good.
4. Stability over flashy: hysteresis intended (broken); state machines; WAIT-heavy outputs (scan shows mostly WAIT/NO-TRADE)
