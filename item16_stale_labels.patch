diff --git a/tests/test_golden_path.py b/tests/test_golden_path.py
index c184d65..c9f0bab 100644
--- a/tests/test_golden_path.py
+++ b/tests/test_golden_path.py
@@ -83,12 +83,17 @@ V3, AND WHY EACH PART OF IT
 
 WHAT THIS BASELINE MUST NOT DO
 ------------------------------
-Two tests in this file are EXPECTED TO FAIL. They document the hardcoded "AERO"
-string and the doubled "relationship relationship", both scheduled for sequence
-item 12. The snapshot records the engine's current output, defects included —
-that is what a baseline is — but the two named defects have their own failing
-assertions so that "the snapshot matches" can never be mistaken for "the output
-is correct".
+Two tests in this file used to be EXPECTED TO FAIL: the hardcoded "AERO" string
+and the doubled "relationship relationship", both scheduled for sequence item
+12 as of v3. Sequence item 12 landed before the item 16 audit began and fixed
+both — verified 1 September 2026 by checking that both now pass on this
+baseline rather than assuming the label was still accurate. They stay in this
+file as regression guards, not as a record of an open defect: the snapshot
+records the engine's current output, defects included — that is what a
+baseline is — but a defect this specific gets its own assertion so "the
+snapshot matches" can never be mistaken for "the output is correct", whether
+that assertion is failing (a live defect) or passing (a fixed one still worth
+watching).
 
 A baseline captured while something is quietly broken pins the broken behaviour
 as the reference. That happened for real on 30 August: the chart renderer's NaN
@@ -516,7 +521,7 @@ def test_a_supertrend_flip_against_prior_state_is_reported():
 
 
 # ============================================================
-# Known defects, scheduled for sequence item 12
+# Fixed by sequence item 12, before the item 16 audit -- kept as guards
 # ============================================================
 
 def test_explanation_does_not_name_a_hardcoded_symbol():
@@ -544,7 +549,12 @@ def test_explanation_does_not_name_a_hardcoded_symbol():
     Found 2026-08-28 by running the engine on TESTUSDT and reading the panel.
     Missed by all four audit runs, because none of them ran it.
 
-    EXPECTED TO FAIL until sequence item 12.
+    STALE-LABEL RE-AUDIT, 1 September 2026: this docstring said "EXPECTED TO
+    FAIL until sequence item 12" from v3 onward. Item 12 fixed both hardcoded
+    sites before the item 16 audit engagement began, and this test has been
+    passing since -- confirmed here, not assumed, by checking the actual
+    result rather than trusting the label. Kept as a regression guard: if
+    AERO is hardcoded back in, this is what catches it.
     """
     if not _engine_available():
         pytest.skip("pandas_ta not installed")
@@ -577,7 +587,10 @@ def test_correlation_phrase_is_not_doubled():
     Cosmetic, but it appears in output the trader reads, and it is one line to
     fix.
 
-    EXPECTED TO FAIL until sequence item 12.
+    STALE-LABEL RE-AUDIT, 1 September 2026: this docstring said "EXPECTED TO
+    FAIL until sequence item 12" from v3 onward. Item 12 fixed the doubled
+    word before the item 16 audit engagement began, and this test has been
+    passing since -- confirmed here, not assumed. Kept as a regression guard.
     """
     if not _engine_available():
         pytest.skip("pandas_ta not installed")
