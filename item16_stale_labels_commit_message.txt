Fix stale "EXPECTED TO FAIL until sequence item 12" labels

tests/test_golden_path.py had three places (the module docstring, a
section header, and both individual test docstrings) still saying two
tests were EXPECTED TO FAIL pending sequence item 12. Item 12 landed
before the item 16 audit engagement began and fixed both defects (the
hardcoded "AERO" string and the doubled "relationship relationship"); both
tests have been passing on every run this session, including every run in
this patch series. The label was simply never updated when the fix
landed.

Not a functional change -- no assertions changed, no `xfail` marker was
ever present, both tests were plain passing asserts with a docstring that
stopped being true. Fixed because a test file whose whole reason for
existing is catching claims that don't match reality ("a test that returns
is a test that passed") should not itself carry a claim nobody re-checked.

Updated, each confirmed against the actual passing result rather than
assumed:
- The "WHAT THIS BASELINE MUST NOT DO" section (was: "Two tests... are
  EXPECTED TO FAIL"; now explains both were fixed by item 12 before item
  16 began and are kept as regression guards).
- The "Known defects, scheduled for sequence item 12" section header (now:
  "Fixed by sequence item 12, before the item 16 audit -- kept as
  guards").
- test_explanation_does_not_name_a_hardcoded_symbol's docstring.
- test_correlation_phrase_is_not_doubled's docstring.

Verification: 137 passed, 0 skipped, 0 failed -- unchanged from before this
patch, as expected for a docstring-only change. Golden snapshot untouched.

This closes out the last item I'm aware of from this whole engagement.
"The owed document batch" mentioned in the roadmap's status diagram is
still unscoped and waiting on Viktor.
